# Copy to .env.local and fill in your Stripe secret key (Dashboard → Developers → API keys)
STRIPE_SECRET_KEY=sk_test_...

# Optional: the public URL of the site (used for Stripe redirect URLs in production)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
